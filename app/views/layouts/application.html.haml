!!!

%html{ lang: 'en' }

  %head

    = favicon_link_tag

    %meta{ charset: 'utf-8' }
    %meta{ 'http-equiv': 'X-UA-Compatible', content: 'IE=edge' }
    %meta{ name: 'viewport', content: 'width=device-width, initial-scale=1' }

    %title FDA Food Recall Dashboard

    = stylesheet_link_tag '//cdnjs.cloudflare.com/ajax/libs/leaflet/0.7.3/leaflet.css'
    = stylesheet_link_tag 'application', media: 'all'

    = javascript_include_tag '//cdnjs.cloudflare.com/ajax/libs/leaflet/0.7.3/leaflet.js'
    = javascript_include_tag 'application'

    = csrf_meta_tags

  %body

    #wrapper

      %nav.navbar.navbar-default.navbar-static-top{ role: "navigation", style: "margin-bottom: 0" }
        .navbar-header
          %button.navbar-toggle{ type: "button", data: { target: '.navbar-collapse', toggle: 'collapse' } }
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar

          = link_to 'FDA Food Recall Dashboard', root_url, class: 'navbar-brand'

        %ul.nav.navbar-top-links.navbar-right
          %li.dropdown
            %a.dropdown-toggle{ href: '#', data: { toggle: 'dropdown' } }
              %i.fa.fa-user.fa-fw
              %i.fa.fa-caret-down
            %ul.dropdown-menu.dropdown-user
              %li
                %a{ href: '#' }
                = link_to page_path('about') do
                  %i.fa.fa-question.fa-fw
                  About

        = render partial: 'shared/sidebar'

      #page-wrapper= yield
